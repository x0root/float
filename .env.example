# WebVM Environment Configuration
# Copy this file to .env and fill in your values

# URL or path specifying where the app loads data from disk
DISK_SOURCE=<URL or path specifying where the app loads data from disk>

# Secret key for API authentication (keep this private!)
API_KEY=<secret key for API authentication>
